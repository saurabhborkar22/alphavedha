# VPS serving dependencies only — no ML training libs (torch, transformers, vectorbt).
# Used when building Docker image for VPS with ALPHAVEDHA_DEMO=1.
# Full deps (pyproject.toml) are used for local dev and model training.

# Data
yfinance>=0.2.40
jugaad-data>=0.25
pandas>=2.2
numpy>=1.26
httpx>=0.27
aiohttp>=3.9
pytrends>=4.9

# Database
sqlalchemy>=2.0
asyncpg>=0.29
alembic>=1.13
psycopg2-binary>=2.9
redis>=5.0

# Feature Engineering
ta>=0.11
statsmodels>=0.14
scikit-learn>=1.5

# ML — imported at module level by models/__init__.py (needed even in demo mode)
# torch is installed separately in Dockerfile with CPU-only index to avoid CUDA packages
xgboost>=2.1
hmmlearn>=0.3
mapie>=0.9
safetensors>=0.4

# API
fastapi>=0.111
uvicorn[standard]>=0.30
slowapi>=0.1.9
prometheus-fastapi-instrumentator>=7.0
pydantic>=2.7
pydantic-settings>=2.3

# CLI / Config / Logging
typer[all]>=0.12
pyyaml>=6.0
structlog>=24.1

# Utilities
joblib>=1.4
pytz>=2024.1
python-dateutil>=2.9
tqdm>=4.66
